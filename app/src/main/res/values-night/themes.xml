<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme (Light) -->
    <style name="Theme.AgriConnect" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Primary colors -->
        <item name="colorPrimary">@color/successGreen</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@color/greenAccent</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background & surface -->
        <item name="android:colorBackground">@color/backgroundLight</item>
        <item name="colorSurface">@color/white</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/green</item>

    </style>

</resources>
